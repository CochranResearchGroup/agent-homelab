# Inventory schema

The packaged canonical schema is `src/agent_homelab/homelab.schema.json`. It is loaded through Python package resources so validation also works from wheels and isolated installations.
