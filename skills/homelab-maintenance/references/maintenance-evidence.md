# Maintenance evidence

Report:

- inventory path and schema version
- rendered and installed digest per node
- component image versions
- validation command and result
- backup destination and recovery command
- whether reload occurred
- local, origin, public, and authentication probe results
- any unverified authenticated application behavior

An unchanged plan is valid evidence of no configuration drift, not evidence that certificates, containers, or applications are healthy.
